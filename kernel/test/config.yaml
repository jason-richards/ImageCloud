debug:
threads: 8                          # Threads to use for handling incoming requests
timeout: 1000                       # Number of milliseconds to wait on a blocked incoming socket
socket: "tcp://*:5555"              # ZMQ Response socket address
storage: /mnt/hgfs/VMShare/origin   # Origin storage location (Where photos/video/audio files will be stored)
face_cascade: /usr/share/opencv4/lbpcascades/lbpcascade_frontalface_improved.xml # Facial recognition cascade file
